# myblog

基于 **Hugo + PaperMod** 的个人博客。

## 本地开发

```bash
hugo server
```

默认访问：`http://localhost:1313`

## 构建

```bash
hugo
```

## 目录说明

- `content/`：文章内容
- `layouts/`：自定义模板与局部组件
- `assets/css/extended/custom.css`：自定义样式
- `hugo.toml`：站点配置

